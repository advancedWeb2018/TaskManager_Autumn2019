
@{
    ViewBag.Title = "Забыли пароль?";
}

<div class="checkForm registration">

    @*<div class="headerSmallScreen">
        <a href='@Url.Action("Index", "StartPage")'>
            <img src='@Url.Content("~/Content/img/logo.png")' alt="logo" />
        </a>
    </div>

    @using (Html.BeginForm("ForgotPassword", "Account", FormMethod.Post, new { role = "form" }))
    {
        @Html.AntiForgeryToken()
        <fieldset>
            <legend>Восстановление пароля<span>Если вы забыли пароль - введите email и код с картинки</span></legend>
            <div class="inputWrap">
                @Html.TextBoxFor(m => m.Email, new { placeholder = "Email" })
                @if (!ViewData.ModelState.IsValid && ViewData.ModelState["Email"].Errors.Count != 0)
                {
                    <span class="field-validation-valid text-danger" data-valmsg-for="Email" data-valmsg-replace="true"><span>@ViewData.ModelState["Email"].Errors[0].ErrorMessage</span></span>
                }
                else
                {
                    @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                }
                <i class="far fa-envelope"></i>
            </div>

            <div class="inputWrap">
                <div class="g-recaptcha" data-callback="enableBtn" data-sitekey='@System.Configuration.ConfigurationManager.AppSettings["ReCaptcha.PublicKey"]'></div>
                <span class="captcha text-danger">Подтвердите что вы не робот</span>
            </div>

            <div class="checkFormWrapBtn">
                <button onclick="history.go(-1);return false;" type="reset" class="transparent colorGrey">Отмена</button>
                <button type="submit" class="fullBg validate-captcha" id="buttonRecover">Восстановить</button>
            </div>
        </fieldset>
    }*@
</div>
